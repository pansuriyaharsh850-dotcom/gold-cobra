import React from "react";
import { Lock, User } from "lucide-react";

export default function Login({ username, password, setUsername, setPassword, onLogin, error, loading }) {
  return <main className="flex min-h-[100dvh] items-center justify-center bg-gradient-to-br from-slate-100 to-gray-200 p-4"><section className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl sm:p-8"><div className="mb-8 text-center"><img src="/logo.png" alt="Gold Cobra" className="mx-auto h-20 w-auto object-contain" /><p className="mt-3 text-sm text-gray-500">Project management portal</p></div><form className="space-y-5" onSubmit={onLogin}><Field icon={User} label="Username" value={username} onChange={setUsername} placeholder="Enter username" autoComplete="username" /><Field icon={Lock} label="Password" type="password" value={password} onChange={setPassword} placeholder="Enter password" autoComplete="current-password" />{error && <p role="alert" className="rounded-lg bg-red-100 px-4 py-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded-lg bg-blue-600 py-3 font-semibold text-white shadow-md transition hover:bg-blue-700 disabled:opacity-60">{loading ? "Logging in…" : "Login"}</button></form></section></main>;
}
function Field({ icon: Icon, label, type = "text", value, onChange, placeholder, autoComplete }) { return <label className="block text-sm font-medium text-gray-700"><span className="mb-2 block">{label}</span><span className="relative block"><Icon size={18} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" /><input required type={type} value={value} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} autoComplete={autoComplete} className="w-full rounded-lg border border-gray-300 bg-gray-50 py-3 pl-10 pr-4 outline-none focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-500" /></span></label>; }
